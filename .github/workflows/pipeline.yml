name: EKS DevOps Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: ap-south-2

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Install Terraform
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq unzip
          TERRAFORM_VERSION="1.14.9"
          wget -q "https://releases.hashicorp.com/terraform/${TERRAFORM_VERSION}/terraform_${TERRAFORM_VERSION}_linux_amd64.zip"
          unzip -q "terraform_${TERRAFORM_VERSION}_linux_amd64.zip"
          sudo mv terraform /usr/local/bin/
          rm "terraform_${TERRAFORM_VERSION}_linux_amd64.zip"
          terraform version

      - name: Pre-Cleanup AWS Resources
        run: |
          echo "🧹 Pre-cleanup: removing orphaned resources..."
          REGION="${AWS_REGION}"
          PROJECT="demo"
          
          # Delete EKS node groups
          NODE_GROUPS=$(aws eks list-nodegroups --cluster-name ${PROJECT}-eks --region $REGION --query 'nodegroups[*]' --output text 2>/dev/null || echo "")
          for ng in $NODE_GROUPS; do
            echo "Deleting node group: $ng"
            aws eks delete-nodegroup --cluster-name ${PROJECT}-eks --nodegroup-name $ng --region $REGION 2>/dev/null || true
            aws eks wait nodegroup-deleted --cluster-name ${PROJECT}-eks --nodegroup-name $ng --region $REGION 2>/dev/null || true
          done
          
          # Delete EKS cluster
          aws eks delete-cluster --name ${PROJECT}-eks --region $REGION 2>/dev/null || true
          
          # Detach and delete IAM roles
          aws iam detach-role-policy --role-name ${PROJECT}-eks-cluster-role --policy-arn arn:aws:iam::aws:policy/AmazonEKSClusterPolicy 2>/dev/null || true
          aws iam delete-role --role-name ${PROJECT}-eks-cluster-role 2>/dev/null || true
          
          for policy in arn:aws:iam::aws:policy/AmazonEKSWorkerNodePolicy arn:aws:iam::aws:policy/AmazonEC2ContainerRegistryReadOnly arn:aws:iam::aws:policy/AmazonEKS_CNI_Policy; do
            aws iam detach-role-policy --role-name ${PROJECT}-eks-node-role --policy-arn $policy 2>/dev/null || true
          done
          aws iam delete-role --role-name ${PROJECT}-eks-node-role 2>/dev/null || true
          
          # Delete ALB
          ALB_ARN=$(aws elbv2 describe-load-balancers --region $REGION --names "${PROJECT}-alb" --query 'LoadBalancers[0].LoadBalancerArn' --output text 2>/dev/null || echo "")
          if [ "$ALB_ARN" != "None" ] && [ -n "$ALB_ARN" ]; then
            echo "Deleting ALB: $ALB_ARN"
            aws elbv2 delete-load-balancer --load-balancer-arn $ALB_ARN --region $REGION
            echo "Waiting 60s for ALB deletion..."
            sleep 60
          fi
          
          # Delete target group
          TG_ARN=$(aws elbv2 describe-target-groups --region $REGION --names "${PROJECT}-tg" --query 'TargetGroups[0].TargetGroupArn' --output text 2>/dev/null || echo "")
          if [ "$TG_ARN" != "None" ] && [ -n "$TG_ARN" ]; then
            echo "Deleting Target Group: $TG_ARN"
            aws elbv2 delete-target-group --target-group-arn $TG_ARN --region $REGION
          fi
          
          # Delete S3 bucket
          aws s3 rb s3://my-lb-logs-${PROJECT}-12345 --force 2>/dev/null || true
          
          # Delete security group
          SG_ID=$(aws ec2 describe-security-groups --region $REGION --filters "Name=group-name,Values=allow-my-ip" --query 'SecurityGroups[0].GroupId' --output text 2>/dev/null || echo "")
          [ "$SG_ID" != "None" ] && [ -n "$SG_ID" ] && aws ec2 delete-security-group --group-id $SG_ID --region $REGION 2>/dev/null || true
          
          # Delete VPC and dependencies
          VPC_ID=$(aws ec2 describe-vpcs --region $REGION --filters "Name=tag:Name,Values=${PROJECT}-vpc" --query 'Vpcs[0].VpcId' --output text 2>/dev/null || echo "")
          if [ "$VPC_ID" != "None" ] && [ -n "$VPC_ID" ]; then
            NAT_IDS=$(aws ec2 describe-nat-gateways --region $REGION --filter Name=vpc-id,Values=$VPC_ID --query 'NatGateways[?State!="deleted"].NatGatewayId' --output text 2>/dev/null || echo "")
            for nat in $NAT_IDS; do
              aws ec2 delete-nat-gateway --nat-gateway-id $nat --region $REGION 2>/dev/null || true
            done
            [ -n "$NAT_IDS" ] && sleep 30
            
            aws ec2 describe-addresses --region $REGION --query 'Addresses[*].AllocationId' --output text 2>/dev/null | tr '\t' '\n' | while read eip; do
              [ -n "$eip" ] && aws ec2 release-address --allocation-id $eip --region $REGION 2>/dev/null || true
            done
            
            IGW_IDS=$(aws ec2 describe-internet-gateways --region $REGION --filters "Name=attachment.vpc-id,Values=$VPC_ID" --query 'InternetGateways[*].InternetGatewayId' --output text 2>/dev/null || echo "")
            for igw in $IGW_IDS; do
              aws ec2 detach-internet-gateway --internet-gateway-id $igw --vpc-id $VPC_ID --region $REGION 2>/dev/null || true
              aws ec2 delete-internet-gateway --internet-gateway-id $igw --region $REGION 2>/dev/null || true
            done
            
            SUBNET_IDS=$(aws ec2 describe-subnets --region $REGION --filters "Name=vpc-id,Values=$VPC_ID" --query 'Subnets[*].SubnetId' --output text 2>/dev/null || echo "")
            for subnet in $SUBNET_IDS; do
              aws ec2 delete-subnet --subnet-id $subnet --region $REGION 2>/dev/null || true
            done
            
            RT_IDS=$(aws ec2 describe-route-tables --region $REGION --filters "Name=vpc-id,Values=$VPC_ID" --query 'RouteTables[?Associations[0].Main != `true`].RouteTableId' --output text 2>/dev/null || echo "")
            for rt in $RT_IDS; do
              aws ec2 delete-route-table --route-table-id $rt --region $REGION 2>/dev/null || true
            done
            
            aws ec2 delete-vpc --vpc-id $VPC_ID --region $REGION 2>/dev/null || true
          fi
          
          echo "✅ Pre-cleanup done"

      - name: Terraform Init
        run: terraform init
        working-directory: env/dev

      - name: Terraform Validate
        run: terraform validate
        working-directory: env/dev

      - name: Terraform Apply
        run: terraform apply -auto-approve
        working-directory: env/dev

      - name: Update kubeconfig
        run: aws eks update-kubeconfig --region ap-south-2 --name demo-eks

      - name: Deploy to Kubernetes
        run: |
          kubectl apply -f app/deployment.yaml
          kubectl apply -f app/service.yaml

      - name: Verify
        run: |
          kubectl get pods
          kubectl get svc
